import React from 'react';

export default class CustomDefaultPage extends React.Component{
    render() {
        return(
            <div>
                Hello custom default page
            </div>
        );
    }
}